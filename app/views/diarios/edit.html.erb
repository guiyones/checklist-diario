<% content_for :title, "Editing diario" %>

<div class="flex flex-col w-full items-center bg-gray-100 rounded-xl px-6 pt-10">
    <h1 class="font-bold text-4xl text-gray-700">Editar</h1>

    <div class=">
        <%= render "form", diario: @diario%>
    </div>
</div>
